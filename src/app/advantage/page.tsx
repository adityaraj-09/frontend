import { Empty } from "../projects/page";

export default function AdvantagePage() {
  return (
    <Empty
      title="Unfair Advantage"
      body="Durable Trigger.dev turns, PostgreSQL as source of truth, and Magica tools that survive a refresh."
    />
  );
}
