import { Empty } from "../projects/page";

export default function HelpPage() {
  return (
    <Empty
      title="Help & Support"
      body="One active run per chat. Reload to resume. If credits run out mid-turn, approve the credit waitpoint or send a new message later."
    />
  );
}
